[project]
name = "debezium-data-generator"
version = "0.1.0"
description = "Fake data generator for Debezium CDC demo"
requires-python = ">=3.11"
dependencies = [
    "psycopg2-binary>=2.9.9",
    "faker>=30.8.2",
]
